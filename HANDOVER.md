# Handover — WeekendCart (C:\EcomSite → https://weekendcart.com)

Written 2026-09-19 for a fresh session. Read `CLAUDE.md` and `AGENTS.md` first; this file adds what they do not say.
State at time of writing (end of the 2026-09-19 session): everything below is pushed and, as far as Kishan reported, deployed. Working tree clean except `CLAUDE.md` (Kishan's own uncommitted edit — leave it alone, never stage it).

**NEXT SESSION STARTS WITH section 3 — "The return + refund journey" — BEFORE ANYTHING ELSE. It replaces and widens the old cancel/refund plan. PLAN FIRST: show Kishan the plan and wait for his OK before writing any code. The hidden ₹1 payment-test product (section 4) comes after it.**

---

## 1. Deployment rules

- The agent has **NO access to the production server**. Never ssh/scp/rsync, never `npm run deploy` (it SSHes), never create deploy keys or CI that deploys.
- Kishan deploys by hand. Flow: make the change → gate (section 5) → commit → `git push origin main` **only when he says push** → give him the server command → wait for the pasted output → fix forward if it failed.
- The exact server command (run as `flexyuser`), always given in a block labelled "🖥️ Server par run karo":

  ```bash
  cd ~/ecom.flexypdf.com && bash deploy/deploy.sh 2>&1 | tail -60
  ```

  Rollback for him: `cd ~/ecom.flexypdf.com && bash deploy/rollback.sh`
- Production: VPS user `flexyuser`, app dir `~/ecom.flexypdf.com`, PM2 app `weekendcart`, port 3040, **fork mode, 1 instance** (`deploy/ecosystem.config.cjs`). deploy.sh runs `npm ci`, `prisma migrate deploy`, `npm run db:seed` (essentials only, never demo), build, PM2 reload, health check, auto-rollback.
- **deploy.sh changes need two deploys**: the script is already running (from a copy of itself) when it pulls its own new version, so an edit to deploy.sh takes effect from the deploy AFTER the one that ships it. Anything that must work on the FIRST deploy goes somewhere the old script already calls — that is why the image-cache link is an npm `postbuild` step and not a line in deploy.sh. Deploy with maintenance mode OFF unless he says otherwise.
- Always tell him plainly what changes on the live site the moment a deploy lands, especially anything that could surprise a shopper mid-session.
- He reviews on the live site, not locally. Local DB: Postgres 16 cluster in `.pgdata` on port **5433** (`npm run db:start`; it is stopped at the start of every session). Local admin login: `admin@mayura.in` / `Mayura@2026`. psql: `C:\Program Files\PostgreSQL\16\bin\psql.exe`.
- Browser tests: Playwright lives in `tools/import-supplier-images/node_modules` (own package.json, never in the root one). Load it from a scratch script with `createRequire("C:/EcomSite/tools/import-supplier-images/package.json")`. Serve with `npm run build && npx next start -p 3999`.
- Shell gotcha on this Windows box: long `node -e '…'` / heredocs with quotes or control characters get mangled. Write edit scripts to the scratchpad with the Write tool and run them with `node "$(cygpath -w …)"`. Never round-trip files through PowerShell Get-Content/Set-Content.

## 2. What is done (all pushed)

- **Next 16.3.5** security bump. Duplicate "Images" sidebar link removed.
- **Kishan's WIP committed** (`9e25b99`): Supplier ("Wholesaler") model + admin, PayU ledger (refunds, settlement, `src/services/payu-ledger.ts`), admin error boundaries. Raw PayU payloads gated behind MANAGER (`b87f6f7`).
- **Product fields**: migration `20260919120000_product_cost_and_headline` adds nullable `Product.costPrice` (admin-only, like `supplierId`) and `Product.headline` (page H1, falls back to title). Storefront now actually emits `metaTitle` / `metaDescription` (absolute title, because the layout has a `%s · WeekendCart` template), keywords from tags; JSON-LD uses the real SKU, absolute image URLs, offers only when price > 0, rating only when reviews exist.
- **5 Home & Kitchen products** from Holiday Wholesale: `scripts/seed-home-kitchen.ts` (`--dry-run`, upsert by SKU, re-runs touch copy only, `--remove` scoped and refuses ordered products, stops if a look-alike exists under another SKU). Images in `public/products/<slug>/`. Scraper in `tools/import-supplier-images/`. Read-only report: `scripts/check-catalogue.ts`.
- **Visibility cascade** — THE rule lives in `src/services/visibility.ts`: `visibleProducts(where?)` returns `{ AND: [VISIBLE_PRODUCT, where] }` where `VISIBLE_PRODUCT = { status: "ACTIVE", category: { isActive: true }, subcategory: { isActive: true } }`. It is a FUNCTION that ANDs, never a spread: a spread was silently overridden by a listing's own `category: { slug }` key (the check script caught it). The constant is deliberately not exported. Every storefront product read goes through it: nine reads in `src/services/catalog.ts`, plus `search-docs.ts`, `commerce.ts` (placeOrder re-pricing) and `cart-availability.ts`. Guards: an ESLint `no-restricted-syntax` rule forbids product queries in storefront files outside those four (admin folders exempt), and `scripts/check-visibility.ts` (static; `--db`; `--all-off`) proves it, self-restoring. Hidden category/subcategory ⇒ gone from menu, listings, search, home blocks, related, sitemap, instant search, JSON-LD, `/p/<slug>` and `/c/...` are real **404**, checkout refuses, cart page flags unavailable lines with Remove. **Brands are OUT of the cascade on purpose** (see section 3).
- **Real 404s**: `loading.tsx` removed from `/p/[slug]`, `/c/[category]`, `/c/[category]/[subcategory]` (streaming made notFound answer 200). The two `/c/` pages declare `dynamic = "force-dynamic"` (an empty `generateStaticParams` with every category hidden caused a 500).
- **Group 1 — status consistency**: one treatment on every admin list via `VisibilityPill`, `VisibilityToggle`, `MUTED_ROW`, `VisibilitySummary`, `blastRadius` (in `src/components/admin/ui.tsx`) and `ToggleForm` (`client.tsx`). Green Active / red Hidden-or-Inactive / amber Draft, "Hidden by parent" naming the parent, muted rows, status filters + count summaries, "N · M visible" product counts, confirm with the real number on switches that affect shoppers. Admin-only `success` green tone added to `Pill`.
- **Group 2 — all categories off** proven empty everywhere (`check-visibility.ts --all-off`).
- **Group 3 — maintenance mode**: state is a row in `StoreSetting` (key `maintenance`), read by `src/proxy.ts` via `src/lib/maintenance.ts` with a 5 s in-memory cache on `globalThis` (assumes PM2 fork mode, documented in the file). 503 + Retry-After + noindex. `/admin`, `/admin/login` and every `/api/*` route are never touched (PayU callbacks keep landing); `/track` and `/legal/*` stay open; a signed-in admin previews the real shop. Header button (portal, fixed panel) AND a plain page at Settings → Maintenance. Auto switch-off when "back by" passes (no cron; logged as System `store.maintenance.auto-off`); picker refuses the past, suggests +2 h. Redesigned holding page: real logo, favicon links, inline SVG shopfront, live countdown that reloads into the shop. Kept test: `scripts/check-maintenance.ts <local url>`.
- **Deploy health check**: `/api/health` + `deploy/health.sh` (sourced by deploy.sh and rollback.sh); maintenance can no longer cause a false rollback.
- **Price guard**: `src/lib/price-guard.ts` — warns, never blocks, ACTIVE only, when price < cost or price < 10% of MRP. Shown at save, beside the price field, as a "Check price" marker + filter on the products list, and on the dashboard "Needs attention" card.
- **Group 4 — admin UX**: collapsible sidebar with submenus and rail flyouts (`sidebar.tsx`; fold control is in the HEADER in `shell.tsx`; the rail only applies on windows ≥ 780 px tall); remembered state through `src/components/admin/use-stored.ts` (`useStored`, `useMediaQuery`, built on `useSyncExternalStore` — this repo's lint forbids `setState` inside an effect); `src/components/admin/form-kit.tsx` has `CollapsibleSection` (product form: Basics open, rest shut, per-admin memory, self-opens on an invalid or server-flagged field) and `SaveBar` (sticky top, dirty/saved state, Ctrl/Cmd+S) on every record form; products `FilterBar` holds at all widths. Fixed a pre-existing bug: after one save, React's form reset blanked the controlled category/subcategory selects and every later save was silently refused.
- **`/preview/badges`** (`src/app/(store)/preview/badges/page.tsx`): admin-only (404 otherwise), noindex. Shows the nine-badge set (`src/components/product/badges.tsx`: `ProductBadgeMark`, `ProductBadges`, `badgesFor`, `BrandMark`) and the loyalty crest (`src/components/account/tier.tsx`: `TierCrest`, `TierBlock`, `TierChip`). **Components exist but are wired NOWHERE yet.** Verdict from Kishan was still pending.
- `npm run lint` now runs `eslint --max-warnings=0`.
- **Inactive brand hides the BRAND, never its products.** `shownBrand()` in `src/services/catalog.ts` yields no `brandName` for a switched-off brand and every shopper-facing reader keys off that absence: no brand line on cards, quick view, bag, wishlist, checkout review or the product page, no brand link, no brand filter, no brand in search docs or JSON-LD, no "undefined" in the page title. **Brands stay OUT of the visibility cascade — the comment on `shownBrand()` says DO NOT add `brand: { isActive: true }` to `visibility.ts`: all 130 products sit on one brand.** The admin brand switch confirms with the real product count and says what it does; the brand form says the same. Past order lines keep the brand text they were saved with.
- **Browser-stored lists answer to the cascade.** The store provider (`src/store/store.tsx`) sweeps recently viewed, wishlist, saved-for-later and the bag once per visit (and again after 5 min on navigation) through `sweepStoredProducts()` in `src/services/cart-availability.ts`. Gone products are dropped from state AND storage — except bag lines, which stay flagged on the bag page with Remove (his earlier decision) but no longer count in the header / bottom-nav / account badges (`state.unavailable`). The same sweep blanks a switched-off brand's name on stored lines.
- **Recently viewed** sends only ids; `recentlyViewedCards()` resolves them through `getProductsByIds` and so `visibleProducts()`. It renders the shared `RailScroller` + `ProductCard layout="rail"` (236 px desktop, 152 px phone), so one item is one card. The homepage `ProductGrid` in `showcase.tsx` had the same stretch fault with 1–2 products; its tracks are now fixed.
- **Group 5 wired — approved as previewed.** One set, icon + colour + word, from `src/components/product/badges.tsx` (`ProductBadges`, `ProductBadgeMark`, `badgesFor`, `BrandMark`): product cards (so listings, search, rails, home blocks, recently viewed), product page buy box, quick view, wishlist, homepage Spotlight. The ad hoc "% OFF" / "Sold out" / "Only N left" marks are gone. Loyalty: `TierBlock` (crest + name + points) in `src/components/account/account-nav.tsx`; unknown tier names fall back to text (`isTierName`). `/preview/badges` still exists (admin-only, noindex).
- **View tracking.** Table `ProductViewDaily (productId, day, views)`, migration `20260919180000_product_view_daily` (additive). Deliberately stores nothing about the visitor. `TrackView` fires one `navigator.sendBeacon("/api/view")` per product per browser session (sessionStorage) after the page is up; `src/app/api/view/route.ts` always answers an empty 204, swallows every failure, skips bots and signed-in admins. It cannot slow or break a product page. The admin skip and the once-per-session rule are in the code but were not exercised by a test.
- **Data-driven homepage.** `src/services/home-ranking.ts` (the file's header comment is the spec). Score = 0.55 recent sales (14 d, 7-day half-life, paid and not cancelled/returned, by `Order.placedAt`) + 0.15 views + 0.20 Bayesian rating + 0.10 log(1+reviews), each scaled across the visible in-stock catalogue. **The weights are Kishan's: views must never outweigh sales.** Blocks fill in order — trending (named "shelf" while there is no sales/views signal at all), new (30 days, no score needed), bestsellers (only products that sold), deals (25%+ off), top-rated, few-left — never repeating a product. Min 4 per block; under 20 visible products at most 3 blocks; sizes in whole rows (10/8/5/4), sized by the blocks that can actually be filled. Cached as ids in `StoreSetting` key `home-ranking`, recomputed at most every 15 min by a homepage render (homepage `revalidate = 120`), marked stale by `revalidateStorefront()`; ids re-checked through the visibility rule and stock at render. `src/app/(store)/page.tsx` is now EmptyHome / RankedHome. The file is on both visibility allowlists (ESLint + `scripts/check-visibility.ts`). **Known gap: his own test orders count as sales until the ₹1 test product exists and is excluded.**
- **Image 504s fixed (confirmed by him on live: console clean, cache survived a deploy).** Root cause: every deploy swapped in a fresh `.next`, emptying `.next/cache/images`; ~20 uncached variants at once outlived nginx's 60 s read timeout. Fix: `deploy/link-image-cache.mjs` (npm `postbuild`, acts only when `NEXT_DIST_DIR` is set, never fails a build) symlinks each build's `cache/images` to `shared/next-image-cache` (gitignored, outside any release). `deploy/warm-images.sh [port]` warms the variants the homepage, `/products` and the sitemap's product pages actually offer (up to 1440 px, 3 at a time, 6-min cap, always exits 0, reports every step and the cache size before and after); deploy.sh runs it after the health check. `next.config.ts`: 10 widths instead of 16 (`deviceSizes [480,640,828,1080,1440,1920]`, `imageSizes [64,128,256,384]`), default quality 75 (90 still on the `qualities` list), `minimumCacheTTL` one year, `maximumDiskCacheSize` 1 GB. **Consequence: to replace a product photo, give the new file a NEW NAME** — the same filename can stay cached. Preloads: the home grid no longer sets `priority`; listings preload 2 cards, not 4. No nginx change was needed; `images.unoptimized` stays off.

## 3. FIRST JOB NEXT SESSION — the return + refund journey, made real (plan first, no code until approved)

Kishan's brief of 2026-09-19, kept in his words where it matters. Scope is wider than the cancel/refund gap: the WHOLE return + refund journey must be real and well managed, on the customer side AND the admin side. PayU is live and taking real money.

**RULE ONE, everywhere:** "refunded" may only be shown — to the customer, in the timeline, in emails, in the admin — when money has actually moved. Either a successful PayU Refund row exists, or an admin explicitly recorded a manual/offline refund with a reference, and that must be labelled differently.

**Fix all three paths to obey it:**
1. Returns "Issue refund" — must raise the real PayU refund itself on PayU-paid orders, then mark the return/order refunded.
2. Cancel order — same (the plan already written; it is in section 4).
3. Nothing sets REFUNDED / PARTIALLY_REFUNDED or writes a "refund issued" event without that proof.
The paths must know about each other: the same money can never be refunded twice, and the amounts stay in sync.

**CUSTOMER SIDE — what he cares most about:**
- A customer raising a return/cancel from their order page sees honest states at every step: requested → approved → item picked up → refund raised → refund confirmed by the gateway → money back. Never "refunded" before it is true.
- Realistic expectations shown, e.g. "PayU usually takes 5-7 working days to land in your account." (Do not invent the number: check PayU's documentation and `/legal/refunds`, propose, and ask.)
- Order timeline events and emails at each real step. No email that says the money is back before it is.
- The return reason, the amount and the mode are clear, and so is what happens if we reject a return.

**ADMIN SIDE:**
- The refund button stays available at any order stage (money is money), but warn on a shipped order: "This order is already shipped — have you cancelled the shipment?"
- For COD and hand-verified UPI there is no gateway: a MANAGER-only "record manual refund" form with a reference, clearly marked as not a gateway refund.
- One place in the admin shows what is owed vs what actually went back, so nothing is lost. Flag any old order where the status says refunded but no money moved (he has a few from testing).
- Refund status must be refreshable from PayU and the order page must show where each refund actually is.

**He wants to see, before any code:** the state machine, what the customer sees at each step, which emails fire, and which files change. **No schema change without telling him** (the old plan needed an additive `REFUND_DUE` enum value; a manual-refund record will need somewhere to live too — say so in the plan).

**What is already known from tracing the code (2026-09-19) — verify, then build on it:**
- `refundReturn` → `settleRefund` in `src/services/admin/returns-actions.ts` is DB-ONLY. It never imports the PayU ledger and never calls `initiateRefund`. It sets the return REFUNDED, restocks, sets `Order.paymentStatus` to PARTIALLY_REFUNDED / REFUNDED (and the order RETURNED, with a timeline event "the refund has been issued") and logs "Refunded ₹N via <mode>" — while no money moves and no Refund row is written. `ReturnRequest.refundMode` is a plain string label. Live example: return #4shp9u51 on order WKC-2026-005047 (PayU), ₹6.
- The ONLY real path is the order page's Refunds card: `src/app/admin/(dashboard)/orders/payment-forms.tsx` → `src/services/admin/payments-actions.ts` → `initiateRefund` in `src/services/payu-ledger.ts` (guards: captured, within the ceiling, locked claim against a double send, unanswered-refund block; one-time token, 23 chars max). `recomputeRefundTotals` there (around line 1330) sets the order's payment status only from SUCCESS refund rows — that is the behaviour everything else must adopt.
- The two paths do not know about each other: pressing the Returns button first does not appear to block the real refund afterwards (the guard works off the attempt and its refund rows, not the order flag — not read line by line), and nothing ties the return's amount to the PayU refund.
- `cancelOrder` in `src/services/admin/orders-actions.ts` (around line 127) has the same defect: sets REFUNDED with no gateway call; and a full PayU refund leaves the order CONFIRMED and shippable. Details of the earlier plan are in section 4.
- Not yet looked at, and the plan needs them: the customer's order pages (`src/app/(store)/account/orders/…`, `src/app/(store)/order/[id]/`), how a customer raises a return or a cancel today, the email templates in `src/lib/emails`, and how PayU refund status is polled or called back.

## 4. Next up, then parked

**Section 3 comes first. The cancel/refund plan below is now PART of section 3 (its path 2) — read it as input to that plan, not as a separate job. The ₹1 test product follows once section 3 is done. Everything else here stays parked.**

- **PayU cancel/refund gap (now path 2 of section 3)** — plan approved in principle, not built. Problem: `cancelOrder` (`src/services/admin/orders-actions.ts`, ~line 127) sets `paymentStatus = REFUNDED` with no gateway call, and a full PayU refund leaves the order CONFIRMED and shippable. Agreed plan: add `REFUND_DUE` to the `PaymentStatus` enum (additive `ADD VALUE IF NOT EXISTS`, as the VERIFYING migration did); cancel writes REFUND_DUE and raises the refund through `initiateRefund` after its transaction; `recomputeRefundTotals` moves it to REFUNDED only on PayU SUCCESS and auto-cancels + restocks an unshipped, unbooked order; `advanceOrderStatus` and shipment booking refuse while REFUND_DUE/REFUNDED; order timeline events on raise and confirm; a manual-refund form for non-gateway orders; a read-only report of old CANCELLED+REFUNDED orders with no successful Refund row. Four commits.
- **Hidden ₹1 payment-test product (after section 3)** — recommendation given, not built: one permanent hidden product ("Payment test, do not ship", ₹1, ACTIVE, in its own hidden category) that only a signed-in ADMIN session may buy (today a hidden product cannot be bought by anyone), its orders marked TEST and excluded from revenue AND from the homepage ranking (the sales query in `home-ranking.ts`) and from view counting. Never drop a real product's price (that is how WKC-HOM-0005 sat at ₹5; his 23 order lines on it were all his own tests).
- **Contact page, proposed and NOT built** (2026-09-20 session; the spam-proofing, prefill, auto-reply, admin blocklist and merged contact card were built and pushed — these four were explicitly deferred):
  - *Order-number validation on the form* — when the topic is "Where is my order?" and the number typed matches no order, say so in the field before the message is sent. Saves a whole round trip. The lookup already exists: `submitContact` in `src/services/commerce.ts` now resolves the order to set `orderId`, so the same query answers this.
  - *FAQ deflection above the form* — show the three or four most common answers, keyed to the selected topic. The biggest support-load reducer of the four.
  - *Response time from real data* — the form says "a person reads every message"; the acknowledgement email says the support hours. Neither states a time to reply. `ContactMessage.repliedAt` minus `createdAt` gives the true median; publish that instead of a promise nobody measured.
  - *Past messages for a signed-in customer* — list their own recent messages on the contact page. Stops duplicate sends by design rather than by refusing them, which is the better half of the rate limit.

- **Contact form, known limits of what was built** — the device marker is an httpOnly cookie (`weekendcart_device`, issued by `src/proxy.ts`), so a private window or a cleared cookie jar starts fresh; the IP and email limits are what actually hold. Refused attempts are logged to `ContactAbuse` and visible at `/admin/messages/blocked`; the message body of a refused attempt is deliberately never stored. The auto-reply was verified to fire only on a stored message: the local run pointed at a dead port and confirmed exactly one send attempt, after the accepted submission and none after the refused one.

- **SMTP is live and confirmed working (2026-09-20).** Kishan reported that a test mail **and** the contact auto-reply both arrived in his Gmail inbox — **not** spam — sent from `support@weekendcart.com` through Hostinger. The server's `.env` carries `SMTP_HOST/PORT/USER/PASS` and `src/lib/mail.ts` reads them, falling back to the old `GMAIL_*` pair only if they are unset. Deliverability from the shop's own domain is therefore no longer an open question; treat a mail that does not arrive as a bug in the sending code, not in the mailbox.
- **`prisma/seed.ts` admin password** — it has a hardcoded default admin password and prints the password to the console. Proposed fix, not done: require `ADMIN_SEED_PASSWORD` from env, never print it, and never overwrite an existing admin's password (CLAUDE.md rule).
- **`scripts/reset-catalogue.ts`** — cancelled. He manages the catalogue by hiding. Nothing gets deleted from the DB.
- Also noted, not requested: `npm audit --omit=dev` shows 5 high findings (nodemailer; prisma's mysql2 and deepmerge-ts), all major bumps.

## 4b. Email system (built 2026-09-20)

- Every mail is built on the shared shell in `src/lib/emails/layout.ts` — header, signature block, button, rows, legal footer. A new template supplies the middle and nothing else. Never copy the table scaffolding into a new file.
- Order lifecycle mail goes through **one** dispatcher, `sendOrderMail(orderId, kind)` in `src/services/order-mail.ts`. It is sent-once (`Order.emailsSent`), runs in `after()`, never throws, and refuses to send a mail whose claim the row does not support (the `truthful()` guard — that is Rule One in code). Never call `sendMail` directly for an order.
- Product images in email use a JPEG twin (`*.email.jpg`) because Outlook cannot render WebP. **Run `node scripts/email-image-twins.mjs` on the server after adding product images.** It is idempotent and costs ~0.16 MB for the whole catalogue.
- Order emails link with a signed token (`src/lib/order-token.ts`) because a mail reader has no session cookie. Read-only, one order, no expiry.
- Password reset is real now: `PasswordResetToken`, hash-only storage, 60-minute expiry, single use. The forgot-password page no longer files a contact message.
- Preview every mail at `/preview/emails` (admin only); `?order=<id or number>` renders a real order through the real code path.

## 5. House rules

- Before EVERY commit: `npm run lint` (warnings fail), `npx tsc --noEmit`, `npm run build`. Storefront product-query changes: also `npx tsx --conditions=react-server scripts/check-visibility.ts`.
- **No schema change without telling him first.** Migrations additive only — never reset, drop or rewrite data. `prisma migrate dev` refuses locally (an old migration was edited after being applied) and wants a reset: do NOT reset; hand-write the migration SQL, apply with `npx prisma migrate deploy`, run `npx prisma generate`, and prove agreement with `npx prisma migrate diff --from-config-datasource --to-schema prisma/schema.prisma --script` (must print an empty migration).
- **Nothing deleted.** No category, product or status is changed in code or in a migration; he does that in the admin himself.
- **Separate commits per concern.** Commit and push only when he says so. Commit trailer: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- "Review only" means review only.
- Never add a dependency to the root `package.json` for a one-off tool.
- Scripts that touch a real database: `--dry-run`, only rows they own, scoped `--remove` that refuses ordered products.
- A test that flips state must restore it **inside the same script** (try/finally or an EXIT trap, original saved first). Never "I restored it afterwards".
- Admin-only data (`costPrice`, `supplierId`) must never reach the storefront, JSON-LD, sitemap or a client bundle; when asked, SHOW how that was verified. The five storefront allowlists are named in the schema comment on `Product.supplierId`.
- Never invent policy or facts: warranty text, tier perks, product specs, MRP, promises on the maintenance page. Read `/legal/*` and existing data, propose, ask if unclear.
- Every command block says where it runs (server or Windows). Report plainly, including your own mistakes.
- Design direction: light and calm, off-white, borderless, square corners, ocean blue + gold. Show renders or screenshots, not hex codes.

## 6. Known live-site facts (as he reported them; the agent cannot see live)

- One brand, **WeekendCart**, currently **Inactive**. All **130** products sit under it.
- 130 products: his 5 real appliances (`WKC-HOM-0001`…`0005`, category `home-appliance` "Home & Appliance"), 120 demo products (`WC-XXX-…`, ten demo departments from `scripts/seed-catalogue.ts`), and the 5 new wholesale items (`HE-2638`, `HE-2879`, `HE-0556`, `HE-2471`, `HE-2170`).
- The **demo departments are still ACTIVE**. At one point "Kitchen & Dining" was active while his real "Home & Appliance" was HIDDEN; he said he would fix statuses from the admin himself. Since the cascade shipped, a hidden department's products are 404 and unbuyable.
- The 5 new items are **DRAFT** under the new category `home-kitchen` "Home & Kitchen" (inactive). Exception: he hand-moved `HE-2170` (bamboo trivet) into Kitchen & Dining, set it ACTIVE with stock 55, and said he would move it back himself. Before publishing any of the five he must verify HSN + GST against the supplier's tax invoice (stored: 8211, 3924, 8215, 8539, 4419, all at 18%), the printed MRP (MRP = price for now) and cost prices. Open spec flags: cabbage chopper size/weight conflict (supplier data 29×9×2 cm / 153 g vs its own photo 32.5 cm / ~160 g); multi-blade chopper has no size or weight; trivet pack courier weight 500 g is an estimate.
- `WKC-HOM-0005` Vacuum Cleaner was live at ₹5; he was correcting it in the admin.
- Payments: PayU is the gateway, UPI QR alongside, COD off.
- **PayU is LIVE and working (2026-09-19, done by Kishan on the server, outside the code).** The PayU salt in the server's `.env` had been EMPTY — that, and nothing in the code, is why PayU never appeared at checkout. He rotated the salt, set it, and a real live payment then went through successfully. So: if PayU ever vanishes from checkout again, check that the server's PayU env values are present before reading code. The agent never sees or handles these values; ask him to check. This also means the next session's two jobs (₹1 test product, cancel/refund gap) are now working against a gateway that takes real money.
- The VPS was compromised in 2026-09 (see the agent's memory notes); that is why outbound network from the server is unreliable and why product images are committed files rather than remote URLs.
- As of the end of 2026-09-19 he had deployed through the image-cache fix and confirmed it on live: no 504s, console clean, `shared/next-image-cache` at 1.9 MB and surviving a deploy. The `ProductViewDaily` migration went out with those deploys. The last push of the session (a more talkative `warm-images.sh`, and this file) changes no app code.
