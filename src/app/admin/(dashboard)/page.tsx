import { PageHeader } from "@/components/admin/ui";

/** Replaced by the full dashboard once the module build lands. */
export default function AdminDashboardPage() {
  return (
    <PageHeader
      title="Dashboard"
      description="Sales, orders and stock at a glance."
    />
  );
}
